# Streamlit's own System / Light / Dark switch stays fully in charge of colour.
# The app's stylesheet uses translucent neutrals and inherited text colour, so it
# follows whichever theme the visitor picks. Nothing here pins a palette.
[client]
toolbarMode = "auto"
